---
description: Alias for /program-manager — roadmap/program decomposition and execution stewardship
---

# /roadmap-steward Workflow

> **Invoke with**: `/roadmap-steward`

`/roadmap-steward` is an alias for `/program-manager`.

Read and follow:

```text
.agent/workflows/program-manager.md
```

Use the canonical command name `/program-manager` in durable artifacts, workflow registry entries, and follow-up recommendations. Keep `/roadmap-steward` as an operator-friendly discovery alias for broad roadmap decomposition.
