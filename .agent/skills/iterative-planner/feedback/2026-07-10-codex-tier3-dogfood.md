# Codex Tier 3 Dogfood Ledger - 2026-07-10

Program: `PGM-IVE-TRUST-REPAIR`

Ticket: `T-INTAKE-90439514` - scheduled headless-agent `/safe-change` dogfood on a seeded defect.

## Status

Implementation, deterministic harness proof, one honest real attempt, and the full closeout battery are complete. The harness ticket closes to its bounded contract; the real receipt is FAIL, so North Star L3 remains open under distinctive successor `T-INTAKE-10643BA2`.

## Mechanisms Used

- Mandatory `task_intake.mjs --goal ... --json` entry and `/program-manager` routing.
- Session rules, knowledge base, bootstrap status, persona scan, story/conflict checks, Program Packet checks, work preflight, and recipe resolver.
- One full `/safe-change-power` child plan with substantive EXPLORE and PLAN, six challenged design decisions, a machine-readable verification strategy, and real transition gates.
- Official `promote-safe-plan` linkage before runtime implementation.
- Active assumptions-challenger, config-integrity, traceability, and wiring-auditor PLAN review.
- Eight-source planner truth packet with WARN risks retained in a compact evidence artifact and structured verification-ledger evidence.
- Reuse of L1 temp-git isolation, `plannerSubprocessEnv` lessons, L2 current-contract lifecycle replay, the shared IVE runner, and CLI determinism inventory.
- Deterministic behavior tests for fixture red state, passing simulation, self-claimed success, failed agent, immutable-test tampering, unexpected files, missing gate evidence, no retry, and receipt freshness states.
- Separate manual/weekly credentialed workflow with always-upload evidence and honest failed-job restoration.
- One authenticated Codex invocation with no retry, followed by parent-process countersign and a committed schema-versioned FAIL receipt.
- Full red-team, regression, retro, story, invariant, annotation, migration, Rule 8, IVE, and scoreboard closeout proof.

## Mechanisms Skipped

- A live LLM in PR, push, or required deterministic conformance: intentionally excluded for cost, credentials, latency, and nondeterminism.
- Automatic retry after a failed real attempt: forbidden because one invocation is the receipt unit.
- Transcript grading or an agent-authored verdict: excluded from every success predicate.
- L4 domain work, quant/live claims, vendor comparison, E4, K2, G1 implementation, and the proposed backlog: explicitly out of scope.
- Global persona force-packs: once `intent_contract.plan_shape` was declared `planner-core`, the relevant four packs became authoritative and quant/UX suppression was correct.

## Friction And Findings

- `TIER3-DOGFOOD-F001`: the L3 intake packet retained a generic Tier-1-era scope and omitted the explicit command boundary, one-shot nondeterminism policy, independent countersign, deterministic/advisory scheduling split, receipt contract, and conditional rung closure. Bootstrap also initially inferred the design-heavy planner integration as a chore until the intent contract overrode it.
- `TIER3-DOGFOOD-F002`: the Active Mistake Response parser requires the exact header `Planned evidence`; the semantically equivalent `Evidence` header produced no guard facts and 64 ontology violations, while the repair surface did not name the missing header contract.
- `TIER3-DOGFOOD-F003`: the live planner truth packet produced about 1.1 MB and took longer than a normal command yield, but the CLI has no output-path or compact mode. Durable capture required waiting for the still-running session and mechanically compacting the result without hiding its six risks.
- `TIER3-DOGFOOD-F004` through `F008`: real-run and red-team evidence exposed bootstrap seed drift, subject/grader runtime sharing, mutable-KB replay, FI title loss, and mtime receipt ordering. Four mechanisms are regression-guarded; mutable-KB replay remains successor scope.
- `TIER3-DOGFOOD-F009`: scoreboard treated the intentionally non-required freshness WARN as both a command failure and warning-count regression. It now reports attributed advisory warnings without failing, while unclassified warning growth remains fail-closed.

Structured findings: `reports/ive/dogfood_findings/2026-07-10-codex-tier3-l3.json`.

## Deterministic Proof

- Harness behavior: 37 passed, 0 failed.
- IVE L3 harness phase: PASS, required suite selected without an LLM.
- IVE runner registry: 471 passed, 0 failed after L3 phase-selection coverage.
- CI workflow contracts: 99 passed, 0 failed.
- CLI determinism: 708 passed, 0 failed.
- Scoreboard contract: 137 passed, 0 failed.
- Rule 8 ripple: 8/8 gates, 0 gaps; migration: 326/326.
- Authoritative full IVE: 118 PASS, 1 attributed advisory WARN, 0 required failures; IVE scores `quality_score=0.9979`, `iv_score=1`, `ritual_score=1`.
- Final live scoreboard: PASS, 0 regressions; scores `quality_score=0.9689`, `iv_score=0.8389`, `ritual_score=1`.

## Claim Boundary

One passing real receipt demonstrates the autonomous-coding loop end to end for the canonical fixture in this harness. It does not prove general autonomous coding, L4 domain work, quant or live-system validity, or vendor superiority. The harness countersigns outcomes independently, but it is not a hostile filesystem sandbox or tamper-proof execution environment.

## Final Receipts

- Real attempt: `reports/ive/autonomous_dogfood_runs/2026-07-10/l3-2026-07-10T11-01-52Z-7fdf8ae2.json` - FAIL after one invocation; RED to GREEN and recorded CLOSE, but path/current-replay countersign rejected it.
- Full IVE: `reports/ive/test_runs/ive-2026-07-10T12-10-40Z/manifest.json`.
- Final scoreboard: `reports/ive/scoreboard/tier3-l3-2026-07-10-final/scoreboard.json`.
- Ticket lifecycle: close `T-INTAKE-90439514` for harness scope in the evidence commit. Keep `US-092` partial, L3 open, and `T-INTAKE-10643BA2` proposed.

## Attempt 5 Harness-Repair Follow-Up

Scope: `T-INTAKE-10643BA2`, child plan `plans/plan_2026-07-10_6f2d19c71d97f345`. No credentialed agent was invoked in this follow-up.

Attempt 5 (`reports/ive/autonomous_dogfood_runs/2026-07-10/l3-2026-07-10T14-56-30Z-b4ec0543.json`) repaired the seeded defect red → green, preserved the test, bootstrapped a real plan, passed all five state-changing transitions to CLOSE after honest retries, satisfied close signals, and made a story-linked commit. Its countersign failures exposed three harness contracts rather than an implementation failure:

- The composed task stopped its explicit lifecycle at CLOSE and did not publish the required audit-only `notify-user` command.
- The task did not name one authoritative immutable-input list, and the fixture did not pre-suppress the known clamp-keyword J5 misfire; the agent therefore edited `audit.config.json`.
- Rule 11 replay proved the recorded fixture salt passes in the retained fixture. The false `GATE-EXP-010` arose because imported `verify_gate.mjs` had captured the parent cwd/knowledge directory before it received an absolute fixture plan path.

Harness repairs now publish all six transition commands verbatim, declare config/ignore/story/tests/knowledge immutable, seed only `quant` and `quant_betting` suppression in the fixture, and evaluate current parent-controlled gate code in a fresh process rooted at the replay repository. Behavioral proof accepts a valid fixture-local KB digest, rejects an incorrect digest, and proves fixture grader mutation is non-authoritative. Receipt JSON is now a tracked proof surface with D5 positive/negative probes. The runbook records the shell-safe Claude `--allowedTools='...'` equals form and a one-hour timeout for the roughly 45-minute ceremony.

The attempt-5 clamp keyword misfire is attached as in-the-wild evidence to `T-INTAKE-E0924D5F`; this follow-up does not implement J5. `US-092` remains `PARTIALLY_COVERED`, and `T-INTAKE-10643BA2` remains `in_progress`. Operator action: trigger exactly one credentialed attempt 6, commit its receipt, and close the ticket only if every strict parent countersign is green.

Focused proof at implementation time: autonomous harness `55 passed, 0 failed`; committed lifecycle replay `21 passed, 0 failed`; D5 proof-surface gate `70 passed, 0 failed`. Full battery and named scoreboard scores are recorded in the child-plan verification receipt after validation.

## Attempt 6 Ignition Follow-Up

Scope: `T-INTAKE-10643BA2`, child plan `plans/plan_2026-07-11_74b4ec2d13d42fc7`. No credentialed agent is invoked in this follow-up.

Attempt 6 (`reports/ive/autonomous_dogfood_runs/2026-07-11/l3-2026-07-11T08-14-26Z-9cf5fcf1.json`) fixed the seeded defect, preserved the immutable test, completed all six gates including `notify-user`, changed no unexpected path, reached CLOSE, and ran for 51.6 minutes. The sole countersign failure was GATE-REF-016 on final-state replay.

Rule 11 diagnosis disproved fact regeneration: both retained final ontology snapshots contain zero mutually-exclusive facts, so no later serializer dropped a fact that existed at reflect-to-validate. The late VALIDATE edit added planner-owned `config/source_hygiene.json`; proof telemetry converted that path into an unscoped host `config_flags_changed` task signal and semantic substrate combined it with the unrelated clamp fixture source. Classification is a parser-normalization/provenance false positive closest to hypothesis (ii), not an agent bookkeeping gap. The repair filters planner-owned evidence paths at task-signal derivation and retains a firing real-host-config control. No magic task-prompt sentence is added, and GATE-REF-016 is not weakened.

The same follow-up adds schema-backed `awaiting_external_action` lifecycle metadata for the honest terminal wait. T-INTAKE-10643BA2 stays `in_progress`; shipped-open is exempt only while no schema-versioned PASS receipt exists under the L3 receipt root. A matching receipt automatically expires the exemption and restores shipped-open reconciliation until the ticket is advanced. Operator action after the local child plan closes: trigger exactly one credentialed attempt 7, commit its receipt, and close the ticket only on a fully green parent countersign.
