# Negative-Scope Analysis Intake Dogfood Ledger — 2026-07-13

Program: `PGM-GUIDANCE-FIRST`

Regression ticket: `T-INTAKE-E0BEA761`

Predecessor: `T-INTAKE-5D4DF8FD` (preserved closed)

## Original Failure

Before mandatory regeneration, `plans/guidance_packet.json` and `.md` held the exact imperative analysis reproduction. The packet classified “without implementing it” as implementation intent, selected full `/safe-change`, published six gate contracts, and included irrelevant Program/persona obligations. This is a behavioral regression in the proportional analysis/question skip contract, not a request to implement the recommended next move.

## Guidance-First Mismatches

1. Mandatory task intake for the repair correctly published full planner-core obligations, but the generated child focus contract dropped its authoritative persona and Program context.
2. Program Manager intake rejected the documented canonical `PGM-GUIDANCE-FIRST` ID form and accepted the packet path.
3. `plan-to-execute` accepted the planner-core intent override in the JS persona gate, while the Prolog fact path re-read the broader plan prose and demanded a quant optimization-scale contract. The false trigger came from the plan's own sentence saying the quant pack was suppressed plus the ordinary verb “model” in a finding. Rewriting those two meta-descriptions without domain keywords made both layers evaluate the same non-domain scope; no fake quant contract was added.
4. After that repair, JS accepted the plan's four explicit learned-obligation guard rows while Prolog required a passing structured `planner_truth_packet` record before EXECUTE. At `validate-to-close`, JS and the green verification table also passed while Prolog required criterion-level ledger subjects synthesized from `verification_strategy.yaml`. The live packet and criterion artifacts were linked through `verification_ledger.json`; WARN evidence and failed pre-evidence probes were not relabeled green.
5. The generated active-mistake hook named deleted legacy `test_migration.mjs`, while the deterministic test census marks it `broken_not_worth_repair` and the current planner-on-planner contract requires governed `migration-bootstrap` and `transition-gate-flows` IVE suites. The child plan was corrected to the governed contract; dead test furniture was not restored.

All five are retained as FI findings. They do not expand this ticket beyond negative-scope classification and terminal skip-packet enrichment.

## Baseline

The untouched full IVE battery passed `123/123` at `reports/ive/test_runs/ive-2026-07-13T08-51-41Z/manifest.json` with `quality_score=1`, `iv_score=1`, and `ritual_score=1`.

## Claim Boundary

This repair will prove route/classification and emitted packet behavior. It will not implement G3, G4, state compaction `T-INTAKE-BD80068F`, the analysis recommendation itself, or unrelated metric repair.
