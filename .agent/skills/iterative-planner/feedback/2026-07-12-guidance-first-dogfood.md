# Guidance-First G1 Dogfood Ledger — 2026-07-12

Program: `PGM-GUIDANCE-FIRST`

Ticket: `T-INTAKE-120A6B61`

## Entry Receipt

The mandatory `task_intake.mjs --goal ... --json` call routed to a full `/safe-change` path but returned only a route. It omitted gate contracts, persona obligations, ontology findings, knowledge entries, and Program/ticket context. Its rationale called the work simple while `simple_task_shape` was false.

## Gaps Encountered Before Implementation

1. `task_intake` did not recognize explicit G1/G2 Program scope from the declared goal and provided no acceptance, verification, dependency, lifecycle, or story-linkage rows.
2. Bootstrap interpreted the implementation ticket as `program_intake` with `pending_focus`, leaving every relevant planner-core persona advisory until `intent_contract.plan_shape` was declared.
3. `work_preflight` published only the five state-changing gates, omitting the audit-only `notify-user` gate.
4. `context_packet --program --ticket` force-included G1 but left the ticket summary empty, admitted unrelated visualizer stories through the ambient dirty payload, and rendered a duplicated Program source path.
5. The Program Packet itself used generic draft acceptance/verification prose, no ticket story refs, and unknown epic story `US-INTAKE-TBD`.
6. The first EXPLORE gate evaluated stale `unknown` authority before refresh and then blocked on missing Program Context that the missing intake packet should have supplied.
7. The first PLAN gate reproduced attempt-6 denial classes: missing deliverable mapping, story linkage, exact verification proof family, and declared M-001 guards.

## Attempt-6 Denial Baseline

The retained L3 decision log yielded nine authoring denial classes that are now first-class packet shapes:

- assumption ledger;
- assumption probe;
- Files To Modify;
- criterion/story linkage;
- seven-column verification matrix;
- mitigation section;
- substantive red-team vector shape;
- complete progress;
- test evidence or structured waiver.

The packet also carries `GATE-REF-016`: real host config contradictions require semantic facts, while planner-owned evidence paths retain their provenance and do not manufacture host mutual-exclusion obligations.

## Claim Boundary

Behavioral tests prove the guidance content that could preempt the measured denial classes. Only a later operator-triggered L3 run can measure an actual reduction from the attempt-6 baseline; no credentialed run belongs in G1/G2 implementation scope.

## G2 Dogfood After G1 Closed

8. The G2 packet correctly carried all six gates, exact Program rows, current invariants, bounded knowledge, and persona suppression, but the active `assumptions_challenger` phase text still included model/calibration actions irrelevant to a planner-core documentation task. This is G1 relevance debt for a later scoped refinement; G2 does not mutate persona packs.
9. The G2 packet routed the goal to full `/safe-change-power` and emitted `bootstrap.mjs new "<goal>"`, but executing the same concrete goal was classified as a chore and refused the user-required child plan. The explicit `--force` recovery succeeded. Route-to-bootstrap agreement remains a measurable G1 follow-up.
10. The packet's ontology/Program context accurately warned that G2 lacked story refs; matching `US-073` in the same packet made the repair deterministic before the first gate.
